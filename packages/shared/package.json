{
  "name": "@satanet/shared",
  "version": "1.0.0",
  "main": "dist/index",
  "license": "MIT",
  "scripts": {
    "start:dev": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@nestjs/common": "^9.2.1",
    "colors": "^1.4.0"
  }
}
